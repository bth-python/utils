## create_labels_in_all_student_repos.py

Use it to find all repos with a prefix, `python-` and add/update labels in those repos. It need the following .env vars.

```
ORGANISATION=
GITHUB_TOKEN=
REPO_PREFIX=
```
