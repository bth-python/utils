name: Decider Workflow

on:
  workflow_call:
    inputs:
      event_name:
        required: true
        type: string
      event:
        required: true
        type: string
      pr_action:
        required: false
        type: string
      review_state:
        required: false
        type: string
      course_id:
        required: true
        type: string
      available_assignments:
        required: true
        type: string

jobs:
  on-pr-opened:
    if: ${{ inputs.event_name == 'pull_request' && inputs.pr_action == 'opened' }}
    uses: bth-python/utils/.github/workflows/examiner.yml@main

  on-need-fix:
    if: ${{ inputs.event_name == 'pull_request_review' && inputs.review_state == 'changes_requested' }}
    uses: bth-python/utils/.github/workflows/need_fix.yml@main
    with:
      review: ${{ toJson(fromJson(inputs.event).review) }}
      course_id: ${{ inputs.course_id }}
      available_assignments: ${{ inputs.available_assignments }}
    secrets: inherit

  # on-push-bth-submit:
  #   if: ${{ inputs.event_name == 'push' }}
  #   uses: ./.github/workflows/workflow-for-push.yml

  # on-other:
  #   if:
  #     ${{ !(inputs.event_name == 'pull_request' && inputs.pr_action == 'opened')
  #     && !(inputs.event_name == 'pull_request_review' && inputs.review_state == 'submitted')
  #     && !(inputs.event_name == 'push') }}
  #   uses: ./.github/workflows/workflow-for-other.yml
